type PreviousProject = {
  id: string;
  title: string;
  description: string;
  images: any;
  htmlContent: string;
};

type NewPreviousProject = {
  title: string;
  description: string;
  images: any;
  htmlContent: string;
};

type EditedPreviousProject = {
  id: string;
  title: string;
  description: string;
  images: any;
};
