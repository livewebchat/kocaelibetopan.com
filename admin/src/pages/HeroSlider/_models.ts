type Slider = {
  id: string;
  title: string;
  description: string;
  image: string;
};

type NewSlider = {
  title: string;
  description: string;
  image: any;
};

type EditedSlider = {
  id: string;
  title: string;
  description: string;
  image: any;
};
