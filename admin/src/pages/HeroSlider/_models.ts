type Slider = {
  id: string;
  image: string;
  title: string;
  description: string;
};

type NewSlider = {
  image: any;
  title: string;
  description: string;
};

type EditedSlider = {
  id: string;
  image: any;
  title: string;
  description: string;
};
