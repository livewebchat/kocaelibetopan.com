type Slider = {
  id: string;
  title: string;
  description: string;
  image: string;
  link: string;
};

type NewSlider = {
  title: string;
  description: string;
  image: any;
  link: string;
};

type EditedSlider = {
  id: string;
  title: string;
  description: string;
  image: any;
  link: string;
};
