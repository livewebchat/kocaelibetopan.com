type Service = {
  id: string;
  title: string;
  description: string;
  images: any;
  htmlContent: string;
};

type NewService = {
  title: string;
  description: string;
  images: any;
  htmlContent: string;
};

type EditedService = {
  id: string;
  title: string;
  description: string;
  images: any;
  htmlContent: string;
};
